eureka:
  client:
    service-url:
      defaultZone: http://localhost:8080/eureka
    register-with-eureka: true
    fetch-registry: true

testing:
  client: "Hello, this is testing from the client side."
